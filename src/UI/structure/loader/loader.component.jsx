import React from 'react'
import {LoaderWrapper, Spinner} from "./loader.styles"
const Loader = () => {
  return (
    <LoaderWrapper>      
      <Spinner/>
    </LoaderWrapper>
  )
}

export default Loader
